{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/compiled.css",
    "test": "vitest run",
    "build": "npm run build:css && HUGO_NODE_BIN=$(which node) hugo --gc --minify",
    "dev": "npm run build:css && HUGO_NODE_BIN=$(which node) hugo server -D --bind 0.0.0.0",
    "prod": "npm run build && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "autoprefixer": "^10.4.24",
    "daisyui": "^4.12.24",
    "glob": "^13.0.6",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^3.4.19",
    "vitest": "^4.0.18"
  }
}
